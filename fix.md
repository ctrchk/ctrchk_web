# 系統修復與用戶操作說明文件 (fix.md)

本文件紀錄了本次版本更新 (v2.2.4 Beta) 所修復的系統問題、新增的功能細節以及用戶操作注意事項。

---

## 🛠️ 修復與改進項目總覽

### 1. 導航頁面 (`nav.html`) 載入中修復
- **問題**：部分裝置或網絡延遲下進入導航頁面時，畫面停留在「系統初始化中...」載入遮罩。
- **修復**：加入了地圖載入錯誤監聽 (`map.on('error')`) 以及 3.5 秒安全超時解鎖機制 (`setTimeout`)，確保不論地圖載入狀況如何，遮罩皆能順利隱藏。

### 2. 騎行頁面 (`ride.html`) 地圖 API KEY 提示與圖層修復
- **問題**：騎行頁面地圖偶發出現 "API KEY REQUIRED" 或底圖載入失敗。
- **修復**：將底圖瓦片切換為免 API Key 的 CartoDB 高清圖層，並加入 `tileerror` 自動觸發 OpenStreetMap 備用源降級機制。

### 3. 暫停路線後繼續騎行恢復
- **問題**：在騎行頁面中選擇暫停並於重開後點擊「繼續騎行」無法實際恢復繪製與計時。
- **修復**：修正了暫停紀錄恢復後啟動計時器、GPS 監聽器與 HUD 說明的觸發函數 `beginRide()`。

### 4. Bug 回報按鈕自由拖曳與自動貼合靠邊
- **問題**：PWA 懸浮 Bug 回報按鈕位置固定，可能遮擋特定頁面的按鈕或地圖。
- **修復**：重構按鈕事件，支援跨平台手勢/滑鼠自由拖曳。放手後，按鈕會根據當前位置平滑自動貼合至螢幕左側（`12px`）或右側（`12px`）邊緣。

### 5. Google 登入按鈕 Keep-Alive SPA/iframe 相容
- **問題**：在 SPA 模式或 iframe 加載時，Google 登入按鈕因容器寬度未就緒而消失。
- **修復**：增加了容器寬度防禦計算與定時重試渲染機制，保證 Google 登入按鈕在任何情況下皆能正常顯示。

### 6. 金卡暴雨雷達圖 (`weather.html`) 載入修正
- **問題**：金卡會員暴雨雷達卡片長時間顯示「雷達數據載入中...」。
- **修復**：修正了天文台 JSON 中圖像路徑的正則提取，並加入 `onerror` 自動備用至天文台最新 128km 雷達圖像。

### 7. 騎行/導航時底部導航欄遮擋 Dashboard
- **問題**：騎行或導航開始後，PWA 底部導航欄會遮擋住底部 HUD dashboard 的按鈕與數據。
- **修復**：當騎行/導航開始時，自動向頁面及父框架 body 添加 `.is-navigating` 類別，強制將底部導航欄隱藏 (`display: none !important;`)。

### 8. 騎行完成後里程頁面 (`mileage.html`) 數據同步
- **問題**：騎行結束後切換至里程頁面，365天滾動里程與 Elite Score 沒有同步刷新。
- **修復**：為 `mileage.html` 加上 `visibilitychange`、`focus` 與 `pageshow` 事件監聽器，切換至該頁時自動重新向伺服器拉取最新用戶數據。

### 9. 跨設備登入條款與細則同意狀態持久化
- **問題**：同一帳號在不同設備/瀏覽器登入時，每次都需要重新同意已同意過的條款。
- **修復**：在 `api/login.js` 與 `api/oauth.js` 登入回傳中加入對資料庫 `terms_agreements` 表對版本 `v2.2.0` 的同意記錄查詢，同一帳號登入任何設備皆會自動識別已同意狀態。

### 10. 設置頁面 (`profile.html`) 返回母頁面
- **問題**：設置頁面點擊退出後無法返回「我的儀表板 (`/dashboard.html`)」母頁面。
- **修復**：在設置頁面頂部新增顯式「返回儀表板」按鈕，明確導向 `/dashboard.html`。

---

## 🔔 新功能：天文台氣象警告推送與安全提醒

- **自動警告推送**：當香港天文台發出暴雨、雷暴、酷熱或寒冷等氣象警告時，App 會自動透過 PWA 本地推送通知提醒用戶。
- **將軍澳跨灣大橋動態提醒**：當天文台發出熱帶氣旋/颱風信號（如 1號、3號、8號風球等）時，通知與提醒內容會自動附上「🚨 颱風信號生效中：將軍澳跨灣大橋單車徑可能實施封路措施，請留意最新路況通知」。

---

## 💡 用戶操作注意事項 (User Actions Required)

1. **瀏覽器通知權限**：
   - 若要接收天文台警告與將軍澳跨灣大橋安全提醒通知，請確保在 App 彈出通知請求時點擊「允許 (Allow)」。
2. **清除本機舊快取 (可選)**：
   - 若發現地圖或樣式沒有即時更新，可在設置頁面進行一次重新登入，以同步最新的 JWT token 與 LocalStorage 快取。
