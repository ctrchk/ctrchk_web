# 導航頁面 (nav.html) 說明

## 已完成的修復與更動
1.  **修正地圖空白問題**：將地圖容器 (`#map`) 的 `z-index` 從 `-1` 調整為 `0`，確保其在 `body` 背景之上可見。
2.  **移除退出按鈕**：根據要求，已刪除左上角的「返回/退出」按鈕 (`#btn-nav-back`)。
3.  **地圖權杖**：已確認 `nav.html` 中已填入公開的 Mapbox Token。

## 額外建議與注意事項
-   **全螢幕體驗**：目前導航頁面已針對 PWA 環境優化。
-   **GPS 權限**：頁面載入後會自動請求 GPS 權限。若地圖仍未顯示，請檢查瀏覽器的位置存取權限。
-   **樣式與配色**：導航與里程頁面已全面支援「金卡」專屬配色與深色/淺色模式切換。

如有其他功能需求或發現問題，請隨時通知。
