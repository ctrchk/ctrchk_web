# 更新日誌 (Engineering Logbook)

所有重要的版本更新記錄於此檔案。

---

## v2.2.3 Beta — 2026-08-10

### 全域實時主題同步、3D 導航語音播報、地圖總站篩選與座標精度修正 (Real-time Theme Syncing, TTS Navigation, Map Terminal Selection, and Coordinate Accuracy Fixes)

- **全域實時主題同步 (Instant Real-time Theme Syncing)**:
  - 徹底重構 `js/main.js` 中的主題初始化及同步機制。
  - 將 `'storage'` 事件監聽器擴展至 `appTheme`、`silverThemeDisabled`、`goldThemeDisabled` 與 `user` 鍵。
  - 當用戶於任何分頁、iframe 內切換顏色模式或激活銀卡、金卡特殊會員主題時，其他背景加載的 Keep-Alive SPA 頁面無需重載即可瞬間實時切換顏色主題。
  - 重構 `initAppTheme()` 以統一控制卡級 CSS 樣式類（`rank-silver`、`rank-gold`），保證多端與多頁面主題樣式 100% 絕對一致。

- **地圖 3D 智慧語音導航及 TTS 播報 (Text-to-Speech Smart Navigation)**:
  - 於導航頁 (`nav.html`) 和騎行頁 (`ride.html`) 全面引進 Text-to-Speech (TTS) 語音播報，採用免費、免 API Key 的瀏覽器原生 `window.speechSynthesis` 引擎（適配 `zh-HK` 粵語及繁體中文）。
  - **幾何拐彎語音預報 (Turn-by-Turn Angle Warning)**：在 `nav.html` 中結合 Turf.js 計算前兩段路網的 Vector Bearing 夾角，於距離拐彎處 < 40m 時主動發起語音播報（例如：「前方左轉」、「前方右轉」）。
  - **行人路段與抵達提示 (Pedestrian & Arrival Announcements)**：進入行人路段時精確播報語音警告（「進入行人路路段，請下車推行」），並於接近目的地/站點或成功打卡時播放語音反饋，保障行車安全。
  - **語音去噪與防抖播報 (TTS Anti-spam)**：引入 `lastSpokenText` 靜態語音快取防抖與 15 秒間隔限制，避免相同語音被重複、重疊播放。

- **地圖總站位置篩選與路線分流渲染 (Terminal Map Selection & Route Split Rendering)**:
  - 在路線頁 (`routes.html`) 地圖下方整合優雅的懸浮終端標籤行 (`#app-terminal-chips-row`) 與 `renderTerminalChips()` 控制器。
  - 支持用戶一鍵篩選總站，並實施動態分流渲染：將路線列表拆分為「📍 從 [總站] 出發的路線」與「🔄 途經 [總站] 的路線」，大幅提升多路網接駁交互便利。

- **香港 1980 座標系 EPSG:2326 精度校正與將軍澳搜尋限制 (Coordinate Conversion & TKO Search Filter)**:
  - **1.2 km 座標漂移修復**：修正 `nav.html` 中 HK80 轉 WGS84 的 linear projection 算法。將 false Easting（假東偏）由錯誤的 `835477.98` 修正為標準 EPSG:2326 Spec 中的 `836694.05`，完美解決香港政府地圖 API 搜索結果向東偏移約 1.2 公里的重大歷史 Bug。
  - **將軍澳區域範圍鎖定 (Strict TKO Search Boundary)**：引入 `isInTseungKwanO(coords)` 經緯度範圍邊界過濾器（`lon: [114.23, 114.29]`，`lat: [22.27, 22.34]`），將導航頁（`nav.html`）中的本機車站搜尋與政府地圖 API 全局搜尋結果嚴格過濾、限制於將軍澳 (TKO) 區段，防止非相關港九新界地址對騎士造成視覺混淆。

- **騎行頁淺色模式 HUD 頂部導航欄深色樣式修復 (HUD Navigation Styling Patch)**:
  - 重構 `ride.html` 淺色主題變量與對比度樣式，消除了淺色模式下 HUD 導航頂欄與文字重疊偏暗的色差問題，使其保持高度清晰的可視度。

- **單車路線圖鑑與數據庫同步優化 (Workable Route Catalog)**:
  - 為 `ensureRideTables()` 與 `api/user.js` 後端加入 `catalog_reward_claimed` DDL 數據庫欄位對接，支持跨設備實時同步和防刷防重領。

- **Serverless 函數數量合規審查**:
  - 全站 API 嚴格維持在 12 個 Serverless 服務（`admin.js`、`blog.js`、`chat.js`、`email.js`、`forum.js`、`getHistory.js`、`login.js`、`oauth.js`、`password-reset.js`、`push.js`、`register.js`、`user.js`），符合 Vercel 免費 Hobby 計劃額度限制。

---

## v2.2.2 Beta — 2026-07-30

### 全分頁 Keep-Alive SPA 系統與 iOS 26 Liquid Glass 導航動畫重磅落地 (Task P3-2 Complete)

- **Keep-Alive SPA 核心架構落成 (Task P3-2)**：
  - **極致平穩與高容錯雙平台設計**：保留瀏覽器 Web 端為常規多頁面網頁 (MPA) 以保證最高 SEO 與極速冷啟動；而在 PWA 獨立應用端 (Standalone Mode) 完美解鎖工業級 SPA 系統。
  - **雙端偵測與 iframe 頁面隱藏**：子 iframe 頁面加載時自動注入 `in-spa-iframe` 類，並經由 CSS 全局覆蓋，優雅隱藏子頁面重複的頂部 Header 與底部導航欄，重新適配底部 padding。
  - **100% 進度常駐與 0 毫秒切頁**：主窗口作為 SPA Shell，首個加載的頁面轉為 Native Tab 容器，其餘 Tab 在點擊時動態、懶載入無縫 iframe 並在背景保持活躍 (Keep-Alive)。切換時 100% 留存地圖實例、實時導航、計時器、以及正在進行中的騎行定位與 GPS 線路，完美實現 0 毫秒極速切頁與 100% 行程無損。
  - **父子窗口事件通訊**：子 iframe 內部的所有主分頁及次分頁（天氣、里程等）跳轉點擊均經由 DOM Interceptor 攔截，安全 delegate 調用 `window.parent.switchToTab(targetUrl)`，免除任何瀏覽器黑/白屏閃爍重載。
  - **滑動縮放與手勢滾動恢復**：修復了 iframe 子頁面中 `overflow` 屬性被誤設為 hidden 阻礙手勢滑動與縮放的重大 Bug，重新覆蓋為 `overflow-y: auto !important`，完全解鎖並恢復了長列表（如任務、騎行和進度）在觸控下的原生物理阻尼滾動與縮放。

- **iOS 26 Liquid Glass 彈性滑動氣泡背景與電影級轉場動畫**：
  - **全 Standalone 實例覆蓋**：優化了 Liquid Glass 啟用邏輯，為所有 PWA standalone 用戶強制注入 `liquid-glass` 樣式類，保證全部 Android 與 iOS standalone 安裝實例皆能 100% 解鎖、正確顯示極致流光玻璃導航欄。
  - **底部導航欄滑動液態氣泡 (Sliding Liquid Glass Bubble)**：底部導航欄內動態附著 `.liquid-nav-bubble` 滑動氣泡。採用高階彈性緩動貝式曲線 (`cubic-bezier(0.25, 1.25, 0.5, 1)`) 並顯式鎖定 `top: 0 !important; left: 0 !important;` 絕對原點，點擊切換時氣泡會以帶有物理彈簧感的流體拉伸動畫平滑滑動到新 Tab 後方，呈現極致流暢且對齊精準的 iOS 26 原生體驗。
  - **電影級頁面切換轉場 (Cinematic Tab Transitions)**：為 Tab 切換編寫了高難度的縮放、漸變與位移轉場動畫。切換時舊分頁輕微向內縮小 (`scale(0.97)`) 並漸隱，新分頁伴隨毛玻璃流體擴散與 `translateY` 平滑升起，將視覺舒適度拉到最高。

---

## v2.2.1 Beta — 2026-07-29

### 任務系統數據庫化每週任務正式落地與 Onboarding 閉環流程優化 (Task P1-3 & P1-4 Complete)

- **每週任務落地與動態配置數據庫化 (Task P1-3)**：
  - **後端 API 實現**：
    - 在 `api/user.js` 新增了 `GET?action=get-tasks` 接口，從 `task_definitions` 表動態獲取所有活躍的任務定義，並結合 Neon PostgreSQL 中的 `cycling_history` 實時計算用戶當前的任務完成進度，提供 100% 後端硬核安全保障與降級 fallback。
    - 新增了 `POST?action=claim-task` 接口，驗證用戶的任務進度是否達成。若達成則安全記錄至 `user_task_progress` 避免重複領取，並更新 `user_game_profile` 獎勵 XP 與里程幣（按卡級乘區加成計算），一鍵領取更具爽快感。
  - **前台重構與 bug 修正**：
    - `tasks.html` 徹底廢除前端硬編碼的 `DAILY_TASKS` 與每週任務即將推出的「🚧 置灰卡片」，統一從後端 API 動態讀取渲染每日與每週任務。
    - 任務卡片新增了互動式「領取 (Claim)」按鈕（當進度達成時出現），點擊時呼叫後端 API 完成獎勵劃撥，領取後播放炫麗的彩帶紙屑特效與升級彈窗。
    - 修復了 `tasks.html` 中獲取 `summary-medals` 數量時，因 `ACHIEVEMENTS` 全域變量未定義而產生 `ReferenceError` 導致初始化卡住的嚴重 Bug，改用 `EARNED_BADGE_IDS.length` 完美替代。

- **用戶註冊與個人資料修補流程（Onboarding）閉環優化 (Task P1-4)**：
  - **流暢導引與引導向導 (Interactive Wizard)**：
    - 驗證成功頁 `verify-email.html` 驗證成功後直接跳轉引導用戶前往 `/profile-setup.html` 免費升級，形成更順暢的 Onboarding 閉環。
    - 將 `profile-setup.html` 的普通單頁表單重構為精美的 **3 步引導式向導 (Interactive 3-Step Wizard)**（第一步：基本資料、第二步：騎行經驗、第三步：地區偏好），在每步點擊下一步前進行嚴格的前置必填表單輸入驗證，並結合 CSS 變量使介面完美適配 Light / Dark / Gold 三套主題。
    - 在 `dashboard.html` 頂部增設亮麗高對比度常駐「高級特權未鎖定 🔓」引導卡片（僅限 `junior` 普通會員展示），點擊後引導至 `profile-setup.html`。
  - **頭像裁剪壓縮優化**：
    - 在 `profile.html` 與 `profile-setup.html` 的頭像上傳處理程序中加入了 **HTML5 Canvas 圖片中心裁剪與高質量壓縮算法**：上傳時在前端自動將圖片中心裁剪為 200x200 像素的正方形，並壓縮為 JPEG 0.7 質量，把檔案體積從數 MB 驟降至 15KB 左右，杜絕用戶直接上傳原始相片造成 Vercel 頻寬超限過載。

---

## v2.2.0 Beta — 2026-07-28

### 里程計劃與特權功能群重構落實 (Comprehensive Redesign of Privilege Ecosystem)

- **會員卡級特權權限精確調整 (Task P4-1)**：
  - **單車泊位 (CYCPARKSPACE)**：特權範圍調整至**銅卡**及以上（原本為銀卡專屬）。
  - **無障礙斜坡與Ramp圖層 (CYCRAMP)**：新增為**銀卡**專屬特權（`map_cycramp`）。在 `nav.html` 中動態載入 `/CYCFACI.gdb_CYCRAMP_converted.geojson`，以亮橙色高亮標示斜坡路段，點擊展示詳細位置與描述。

- **銀卡「鈦金銀」主題與3D交互體驗升級 (Task P4-2)**：
  - 全新設計銀卡特權頁面（`mileage.html`）的 3D 擬真卡片傾斜效果，加入平滑的手勢拖拽跟隨及懸停微調。
  - 當用戶觸發或滑動銀卡時，動態在卡面後方生成科技感十足的**銀色雪花與星光粒子特效**。
  - 精心打磨 `body.rank-silver` 主題色彩變量，呈獻高對比度、具科技冷冽感的鈦金銀/鉻金屬感（Titanium/Chrome）高階 UI 視覺。

- **特權狀態全面實現與可行性測試 (Task P4-4)**：
  - **3D車道級導航 (Gold)**：於 `nav.html` 為金卡會員引入霓虹藍立體車道線，具備科技呼吸感律動。
  - **IG/黃金自定義海報 (Silver/Gold)**：升級 `dashboard.html` 海報生成畫布，銀卡/金卡自動移除浮水印。金卡用戶可鍵入自定義文字口號，自定義渲染於高解析度黄金海報上。
  - **連接香港天文台 API 雷達與災害警告 (Gold)**：
    - `weather.html` 整合天文台 live `nradar_img.json` 雷達 API，實時渲染暴雨雷達圖與動態時間軸。
    - 實時獲取天文台暴雨/颱風災害警告（如紅色暴雨、八號風球），若有災害提示，為騎行者提供專屬氣候避險引導與安全提示。
  - **AI 騎行狀態分析報告 (Silver/Gold)**：`GET /api/user?action=ai-report` 整合 Gemini 1.5 Flash。當沒有提供 API Key 時自動啟用基於騎行數據的擬真智能教練模板算法生成。
  - **週末雙倍里程提早體驗 (Gold)**：金卡會員可在星期五 00:00 起提前预览並騎行週末雙倍路線（`hkt_day === 5`），提交騎行時自動獲得 2.0 倍里程幣與 XP 收益。
  - **路面障礙優先審核權 (Silver/Gold)**：報告路障時自動標記為優先（`priority_audit = 1`）。在 `admin.html` 中增設專屬「優先審核面板」，支持一鍵通過或拒絕路障，修改並同步更新至地圖。

- **刪除連勝因子S，全面替代為成就因子A (Task P5)**：
  - 徹底移除通勤連勝因子 `S` (Streak Factor)。
  - 改用全新**成就因子 `A` (Achievement Factor)** 替代：公式為 `Badges_Count * 25`，上限為 150 分。
  - 同步更新 `/api/user` 與 `/api/getHistory` 的計算及寫入邏輯，並完全更新 `docs/mileage.md` 與 `mileage.html` 中的公式與文案描述。
## v2.1.4 Beta — 2026-07-28

### 任務 0 (P0) 工業級穩定性、防刷防作弊與視覺一致性加固正式落地 (Task P0 Complete)

- **導航頁跟隨與 GPS 羅盤抖動優化 (Task P0-1)**：
  - 引入了 2D 卡爾曼濾波算法（`KalmanFilter2D`）對經緯度及速度進行實時平滑，降低噪聲干擾。
  - 實施低速鎖定補丁：當速度低於 3km/h 時，屏蔽來自設備陀螺儀/指南針（Heading）的高頻微幅抖動，將地圖朝向（Bearing）鎖定在當前路網幾何矢量切線上，杜絕眩暈感。
  - 用戶在手動平移/縮放地圖時，觸發 10 秒倒數計時器暫停強制回中，提供高度自適應的手動探索彈性，超時或點擊「跟隨我」後自動恢復鎖定。

- **工作台內存洩漏與 Leaflet 瓦片管理及狀態恢復 (Task P0-2)**：
  - 引入了 Turf.js 的道格拉斯-普克（Douglas-Peucker）軌跡抽稀算法，每 10 秒執行一次，確保長途騎行時追蹤座標不發生無上限膨脹。
  - 實現定期瓦片生命週期管理，每 2 分鐘調用 Leaflet `map.removeLayer` 與重載，徹底消除視野外歷史瓦片緩存對 V8 引擎與 DOM 的內存洩漏，內存控制在 120MB 以下。
  - 新增增量實時持久化：所有未提交的虛線軌跡點和 active ride 狀態以 JSON 格式實時 Append 至 `localStorage` 的 `unsyncedTempCoordinates` 及 `unsyncedActiveRideState`。當 App 崩潰、關閉或意外閃退重啟時，首頁與騎行工作台會彈出引導式 Prompt 完美實現 100% 行程無損恢復與斷點續傳。

- **黑金主題高對比度樣式覆蓋與對比度加固 (Task P0-3)**：
  - 對 `css/main.css` 進行全局覆蓋升級。當 `body.rank-gold` 啟用時，利用強硬的 `!important` 強制全站按鈕、聊天面板、路徑提示、計時器、多人房面板、自訂途經點等交互組件切換為高對比度高亮度黃色（`#F0D372`）與高飽和純黑色（`#000000`）。
  - 優化 Mobile Safari 的彈出式 Modal 及輸入框文字對比度，杜絕烈日暴晒、100,000 勒克斯強光下任何「白底白字」或「黑底黑字」的色差盲區，大幅保障戶外行車視線安全。

- **PWA 離線緩存同步容錯與 Service Worker 重複上傳抑制 (Task P0-4)**：
  - 在 `js/main.js` 的 `syncRideHistory` 中設計並實現了**遞歸式隊列同步鎖（Recursive Sync Queue Mutex Lock）**，採用序列化 Promise 隊列確保同一時間僅有一條本地緩存歷史在進行上載，隔絕並發網絡重試導致的同步雪崩。
  - 對數據庫 schema 執行安全升級，在 `cycling_history` 表中加入唯一性約束 `client_uuid`。後端 `api/getHistory.js` 在接收數據時對 `client_uuid` 進行前置唯一性校驗，若發生重複衝突則優雅冪等攔截，保證 Neon PostgreSQL 排行榜和三軌制數據 100% 精確且不重複。

- **反作弊瞬時速度平滑與單車徑路網擬合算法 (Task P0-5)**：
  - 後端 `api/getHistory.js` 對上傳的 GPS 軌跡進行二次平滑。不再依賴單點時速計算，改為使用**滑動窗口移動平均速度算法（Sliding Window Moving Average, size=5）**，完全過濾由 GPS 瞬時定位漂移引起的假超速誤判。
  - 引入了**單車徑路網覆蓋率擬合算法（Map-matching Coverage）**：對非「自由模式」騎行，利用後端緩存並預加載的全港官方 `CYCTRACK` 單車徑幾何圖形包（Bounding Box 快速過濾），計算軌跡點 50 米範圍內是否處於專用單車網路上。若單車徑覆蓋率低於 60% 則判定為公共交通、公路開車作弊，優雅予以作廢並顯示幽默警告文案，維護全港排行榜誠信。

---

## v2.1.3 Beta — 2026-07-25

### 新功能與體驗加固 (New Features & Life Cycle Hardening)

- **里程過期 30 天預警圖表與可視化正式落地 (Task P1-1)**：
  - **優化內容**：後端 `api/user.js` 新增基於 PostgreSQL 區間查詢的 `approaching_expiry_km` (未來 30 天即將失效的累計里程) 與 `expiry_schedule` (未來 30 天每日即將到期里程的完整列表)。
  - **前台展現**：在 `mileage.html` (里程計劃) 中，全新設計和實現了高對比度、無外部依賴的玻璃擬態 (Glassmorphic) 垂直柱狀圖表，完美自適應 Light / Dark / Black Gold 三套主題。
  - **交互特效**：柱狀圖支持動態過渡柱高動畫，支持移動端與桌面端的觸碰與懸停，觸發 tooltip 精確顯示某日即將到期的具體公里數與日期，極大提升保級和騎行上癮性激勵。

- **Apple & Google Wallet 卡包數據實時秒級同步與 Push 推送 (Task P1-2)**：
  - **數據硬化**：在 `user_game_profile` 表中加入 `wallet_serial` 欄位。當用戶通過 `api/user.js?action=wallet-pass` 下載卡包時，系統自動將 WalletWallet 的序列號 (serialNumber) 進行安全持久化記錄。
  - **秒級更新與 Push**：封裝獨立且高容錯、非阻塞的 `lib/wallet-helper.js?triggerWalletPassUpdate` 異步更新工具。當用戶在 `api/getHistory.js` 提交新騎行記錄後，系統會自動在背景發送 `PUT` 請求向 WalletWallet 更新用戶的最新的滾動里程、卡級以及等級數值，完美觸發手機電子卡包的即時同步與鎖屏更新 Push Banner。
  - **防本地報錯**：Wallet 請求的 `logoURL` 自動適配本地 (localhost / sandbox) 的 GitHub RAW bike icon 備份，避免本地調試發生跨域或 404 獲取失敗報錯。

---

## v2.1.2 Beta — 2026-07-22

### 系統效能優化與 Bug Sweep (Performance Optimizations & Bug Sweep)

- **優化內容**：重構 Leaflet 與 Mapbox 地圖生命週期管理，全面檢查並防止重複初始化（利用全新的 `window.PWA_MAPS_INITED` 作為守衛，地圖在退出前顯式調用 `map.remove()`）。
  - **原因**：避免 PWA 應用因切換頁面或重複初始化地圖產生 `Map container is already initialized` 的 Console 報錯與嚴重的記憶體洩漏（Memory Leak）。
  - **改善效果**：完美解決地圖二次加載的 JavaScript 報錯與崩潰，降低移動端在長途騎行時的內存佔用，保證 App 流暢穩定。

- **優化內容**：全面清理、歸集並銷毀 `setInterval` 與 Geolocation GPS 定位追蹤 `watchId`。
  - **原因**：頁面跳轉或關閉時，後台的定時器與定位追蹤器如果持續運行，會導致手機電量在騎士騎行過程中加速耗盡。
  - **改善效果**：在 `pagehide` / `unload` 事件觸發時徹底清理所有計時器和監聽器，消除後台背景流氓線程，降低移動裝置能耗。

- **優化內容**：加固 Wake Lock 防護與自動重連機制。
  - **原因**：當騎士將 App 置於背景再返回前台時，Wake Lock 鎖可能被系統自動釋放，導致手機熄屏 GPS 信號丟失。
  - **改善效果**：使用 try-catch 包裹 `wakeLock.request` 避免 Promise rejection 崩潰，並引入全自動 `visibilitychange` 重連機制，返回前台時重奪 Wake Lock 鎖，保障屏幕常亮不熄滅。

- **優化內容**：對高頻動畫與 CSS 變更進行 Paint/Reflow 減碳優化。
  - **原因**：`transition: all` 會在多個屬性（如寬高、位置、顏色）變動時引發高強度的重排（Reflow）與重繪（Repaint）。
  - **改善效果**：將關鍵組件的過渡動畫修改為具體的 `transition: transform` 或 `transition: background-color`，顯著降低 CPU/GPU 渲染開銷，提升 mobile Safari 滑動時的 FPS 到 60 幀。

- **優化內容**：Mobile Safari 體驗、對比度及滾動優化。
  - **原因**：iOS 網頁原生滾動存在回彈與阻尼不足，且黑金主題下高對比度文字需要極強的對比。
  - **改善效果**：在 scroll 容器上添加 `-webkit-overflow-scrolling: touch;` 提供媲美 Native 的流暢阻尼感；加強並確保 `isLoggedIn()` 下 `localStorage` user 清洗安全性。

---

## v2.1.1 Beta — 2026-06-26

### 新功能 (New Features)
- **Apple & Google 電子錢包 (Wallet Pass) 正式上線** — 在里程計劃頁面 (`mileage.html`) 整合官方樣式的 "Add to Apple Wallet" 與 "Add to Google Wallet" 按鈕。
- **WalletWallet API 永久免費通道對接** — 成功避開 Apple 開發者帳號費用及 Pass2U 的 30 天試用限制，直接對接到 WalletWallet API 生成卡片。
- **等級自適應卡面與色彩調色** — 後端 (`api/user.js`) 根據用戶 365 天滾動里程等級自動匹配 Gold/Silver/Bronze 模板：
  - **金卡 (Gold)：** 金黃色主題配色 (`#F0D372`)
  - **銀卡 (Silver)：** 科技銀灰色主題配色 (`#D1D9DF`)
  - **銅卡 (Bronze)：** 經典銅褐色主題配色 (`#D8A56B`)
- **品牌 Logo 與資訊整合** — 錢包卡片整合官方 CTRC HK 圖標 (`icon-192.png`)，並支持顯示用戶姓名、滾動里程、卡片等級以及帶有專屬車手 ID 的二維條碼。

---

## v2.1.0 Beta — 2026-06-25

### 新功能 (New Features)
- **多人房間系統正式上線** — 徹底重構多人騎行邏輯。用戶現在可以開設「公開房間」或設有「密碼」的私密房間。
- **房間持續性與恢復** — 多人房間現在支援斷點續傳。若騎行中意外斷線或退出，可透過首頁的「未完成行程」卡片直接重回原有房間，保持與隊友的同步。
- **全港挑戰賽 2.0** — 挑戰賽入口現在獨立於普通路線篩選，位於「單車路線圖籍」標題旁的金色獎盃。
- **「專屬徽章」3D 藝廊** — 任務頁面的成就獎牌更名為「專屬徽章」，並全面升級為 3D 沉浸式展示，支援縮放與旋轉檢視。
- **全新徽章等級** — 新增「累積騎行 50km (耐力達人)」及「體驗者 (單次 5km)」兩項新徽章。

### 系統優化 (System Optimizations)
- **挑戰賽規則加固** — 自由模式不再能解鎖全港挑戰賽；路線 960 已明確從挑戰賽範疇中排除。
- **獨立車站管理系統** — 為全港挑戰賽建立了獨立的車站數據庫與管理介面，支援 CSV 導入與精確的座標校對。
- **導航 GPX 自動選取** — 管理後台現在會自動掃描 `/gpx` 目錄，無需手動輸入檔案路徑，提升管理效率。

---

## v2.0.9 Beta — 2026-06-18

### 新功能 (New Features)
- **多人模式 (Multiplayer Mode) 上線** — 移除舊有的「與朋友一起騎行」勾選框，改為全新的房間系統。用戶可開設公開或私密房間（支援密碼），邀請好友共同出發。
- **房間騎行斷點續傳** — 若在多人房間騎行時意外退出，可透過「發現未完成行程」按鈕重回房間並恢復同步進度。
- **全港挑戰賽入口遷移** — 為提升專業感，全港挑戰賽入口從普通篩選器移至「單車路線圖籍」標題旁的專屬獎盃圖示，點擊即可開啟專屬挑戰賽頁面。
- **3D 徽章管理優化** — 管理員現可直接從 `/model/glb` 及 `/model/usdz` 目錄中選擇已上傳的 3D 模型檔案，不再需要手動輸入冗長的 URL。

### 系統優化 (System Optimizations)
- **多人同步邏輯升級** — 實時位置分享現已完全基於房間成員系統，確保數據隔離與安全性。
- **挑戰賽規則收緊** — 自由模式不再觸發全港挑戰賽獎勵；明確排除 960 路線作為挑戰賽路段，維護挑戰賽的公平性。
- **管理權限整合** — 徽章管理與全港挑戰賽路線設計現已完全整合至 `admin_badges.html`，提供一站式的獎勵與挑戰配置體驗。

## v2.0.8 Beta — 2026-06-15

## v2.0.7 Beta — 2026-06-12

### 新功能 (New Features)
- **「已省車資」系統上線** — 全面取代原有的「連戰/通勤」顯示。系統會根據導航里程或路線完成比例，自動估算您透過單車出行省下的公共交通費用，並在儀表板與報表中實時顯示。
- **3D 導航深度優化** — 修復導航開始按鈕偶發性失效問題；新增「放大/縮小」地圖控制及「跟隨我」模式切換，地圖不再強制鎖定縮放級別；為高級會員恢復了 CYCPARKSPACE 單車泊位圖層顯示。
- **大數據預計時間** — 路線時間現在更智慧！若該路線累計騎行次數超過 100 次，系統將自動採用大數據平均時間，而非單一估算值。
- **管理員綜合報表** — 後台用戶管理現提供 Overall 綜合報表，一站式查看用戶的等級、XP、里程幣、省下車資、騎行時數及高級會員註冊時填寫的詳細經驗與單車資訊。
- **免責聲明更新** — 登入與註冊頁面新增「資訊僅供參考」及「Google 登入即代表同意條款」等重要提示。

### 修復 (Bug Fixes)
- **導航交互修復** — 解決了導航時手動平移地圖後會被強制回中的問題，現在點擊「跟隨我」按鈕即可自由切換鎖定狀態。
- **數據精度優化** — 所有車資相關數據現在統一顯示至小數點後一位，確保視覺簡潔且精確。

## v2.0.6 Beta — 2026-06-11

### 新功能 (New Features)
- **單車路線圖籍 (Route Catalog) 升級** — 在騎行頁面 (`ride.html`) 整合圖籍入口，支援在騎行過程中實時查看進度、玩法說明及區域征服狀態。集齊可領取 **10,000 XP** 及 **1,000 里程幣**，並自動解鎖所有特別篇路線。
- **網頁版視覺優化** — 網頁版路線列表不再套用「未征服灰階濾鏡」，確保非 PWA 用戶也能看到豐富色彩的路線資訊。

### 修復 (Bug Fixes)
- **自由模式功能修復** — 修復 `ride.html` 自由模式 UI 缺失、計時器停止及無法保存行程的嚴重 Bug；優化自由模式結束時的路線相似度匹配算法。
- **環境感知門控** — 徹底隱藏網頁版內的「自由模式」及「開始騎行」入口，引導用戶使用 PWA 體驗核心騎行功能。
- **地圖圖層恢復** — 恢復導航頁面 (`nav.html`) 的 **3D 建築物 (金卡專屬)** 及 **單車泊位 (銀卡/金卡)** 顯示，修正 Mapbox 資料源識別邏輯。

## v2.0.5 Beta — 2026-06-10

---

## v2.0.4 Beta — 2026-06-05

### 新功能 (New Features)
- **自由騎行模式 (Free Ride Mode)** — `ride.html` 新增自由騎行功能，含 3-2-1 倒數計時。系統會自動偵測沿途所有站點並給予 XP，結束時自動匹配相似路線發放獎勵。
- **好友系統 (Friend System)** — `chat.html` 及 `api/user.js` 新增好友功能，支援搜尋用戶、發送/接受好友請求，並可查看好友的等級、里程等狀態。
- **螢幕常亮 (Screen Wake Lock)** — 騎行及導航期間自動啟用 Wake Lock API，防止螢幕自動關閉。
- **快取優化 (Initialization & Caching)** — `index.html` 新增「跳過載入」按鈕；`js/main.js` 實現全域路線及資料快取，大幅提升頁面切換速度。
- **主頁已省車資顯示** — `index.html` 用戶卡片新增「已省車資」顯示。
- **途經點 UI 升級** — `nav.html` 導航途經點功能現對所有用戶可見，銅卡用戶會顯示鎖定圖示及升級提示。

### 修復 (Bug Fixes)
- **定位邏輯修正** — 修正天氣、導航及騎行頁面的自動定位邏輯。現在僅在 App (PWA) 模式下自動要求權限，網頁版改為手動觸發，解決網頁版強制取位及 App 內失效問題。
- **導航地圖跟隨修正** — 修正導航時地圖強制回中的問題。新增手動操作偵測，用戶縮放或平移地圖後會暫停自動跟隨，點擊指南針可恢復。
- **銀卡停車系統修復** — 修正 `nav.html` 中單車泊位圖層的檔案路徑，恢復銀卡用戶的停車位顯示功能。
- **金卡主題相容性修正** — 修正 `profile.html` 中金卡用戶關閉黑金模式後無法切換亮色模式的問題。

---

## v2.0.3 Beta — 2026-06-01

### 新功能 (New Features)
- **關於我們全面升級** — `about.html` 新增完整 App 功能介紹，包含 3D 車道級導航、智慧追蹤、遊戲化成就、社群互動及三軌制體系詳解。
- **三軌制會員體系落實** — `membership.html` 更新為「普通/高級/VIP」三軌制。VIP 會員享有自定義主題等 Creative Rights；詳細對比 XP (等級)、里程幣 (經濟)、里程 (實力/卡級) 的用途。
- **網誌投稿系統** — `blog.html` 與 `api/blog.js` 升級，現支援非管理員用戶投稿（預設待審核），並新增「我的投稿」記錄查看功能。
- **騎行詳情專頁** — 新增 `ride_history_detail.html`。每條騎行紀錄現在擁有專屬頁面，可查看詳細速度、里程、獎勵數據及 Leaflet 軌跡地圖。
- **紀錄救援計畫** — 移除 `api/getHistory.js` 中對短途紀錄的過濾邏輯，確保所有接收到的騎行數據均被完整保留並顯示。
- **導航頁面正式開發完成** - 更新 `nav.html` 連接全新 `/data` 內的單車徑資訊。
- **天氣頁面升級** - 更新 `weather.html` ， 增加更多有用提示，並在主頁增加溫度提示及快捷按鈕。

### 修復 (Bug Fixes)
- **AI 開發模式修復** — 修復管理員頁面 AI Agent 無法正確識別 Repo 分支或生成格式錯誤的問題，優化了檔案樹讀取與提示詞邏輯。
- **網誌顯示修復** — 修正網誌列表未能正確過濾並顯示已發佈文章的問題。
- **修復顏色顯示問題** - 更新部份頁面，修復不同模式中顏色顯示重疊的問題。

---

## v2.0.2 Beta — 2026-05-29

### 修復 (Bug Fixes)

- **PATCH** - 修復導航功能

## v2.0.1 Beta — 2026-05-26

### 新功能 (New Features)
- **客服功能** - 新增全新客服系統，App或網頁內可直接聯絡技術人員。

### 修復 (Bug Fixes)
- **REMOVAL** - 第36行，`/workspaces/ctrchk_web/js/pwa.js` 不可行權益 <br>  
處理方式：移除相關文字
- **PATCH** - 刪除舊文件夾 `/ctrc.login` [已棄用]<br>  
`git rm -r --cached ctrc.login`
`echo "ctrc.login/" >> .gitignore`
`git add .gitignore`<br>  
- **PATCH** - `suspended_ifneed.html`已被移至`.gitignore`

## v2.0 Beta — 2026-05-25

### 新功能 (New Features)
- **三軌制經濟正式落地** — XP（含旅遊/通勤/自由倍率）、365 天滾動里程 Rank（含 Silver/Gold 保級）、里程幣乘區（Silver +5% / Gold +15%）整合到騎行與簽到結算。
- **IG 心理上癮模組上線** — 通勤連勝顯示與 100 里程幣修復、結算盲盒隨機獎勵（XP/里程幣 +1~10）、反作弊作廢（>45km/h 或疑似高速軌跡）與幽默警告文案。
- **完整 25 項航空級權限字典啟用** — 依 Bronze/Silver/Gold 嚴格鎖定權益，前後端以 Permission Context 同步控制。
- **管理員後台路線/總站接軌 App** — 路線管理支援底色、別稱、預計時間、解鎖條件（Level/Coins）與標籤；新增總站管理並同步到騎行頁地圖入口。

### 修復 (Bug Fixes)
- **主頁歡迎卡同步修復** — PWA 首頁歡迎區改為以最新伺服器遊戲進度回寫本機快取，修復 XP/里程幣不同步；同時新增 365 天里程顯示。
- **我的頁面長列表體驗修復** — 會員權益與騎行記錄改為預設顯示部分內容，支援「查看更多 / 收起」。
- **管理員里程幣優惠載入失敗修復** — 補齊 `routes_config.promo_cost` 遷移與 API 相容處理。
- **站點/路線管理儲存與總站切換修復** — 修正總站切換函式作用域與路線/站點欄位遷移，恢復後台儲存並可與 App 騎行頁對接。
- **連勝修復 API 路由修正** — `repair-streak` 由錯誤巢狀分支改為正式 POST action 分支。

### 資料遷移 (Data Migration)
- 新增舊資料庫向前相容遷移：`cycling_history`（stops_count/all_stops/districts_count/anti_cheat/random bonus）、`routes`（alias/bg_color/estimated/unlock/tags）、`stations.is_terminal`、`routes_config.promo_cost`。
- 既有舊帳戶不需重建，部署後自動補齊欄位並沿用原有騎行資料。

---

## v1.11 — 2026-05-24

### 新功能 (New Features)
- **Implemented Admin Station/Route Management System and New Mileage Multiplier Logic (x1.5 / x1.0 / x0.8)** — 新增後台站點/路線動態管理（含站點 CRUD、路線建立與編輯、站點經過路線總覽），並套用新騎行倍率規則：旅遊模式 x1.5、通勤/DRT 模式 x1.0、自由導航模式 x0.8。

---

## v1.10 — 2026-05-23

### 新功能 (New Features)
- **Optimized Custom Dijkstra Cycling Routing Engine (No-Roads Policy)** — `/nav` 路線規劃改為只使用本地 `/data/tko_tracks.json` + 程式化 Virtual Links 建圖，完全移除外部路由引擎依賴；`push-bike` 連接段成本提高至一般單車徑 100 倍，僅在必要連通時使用。
- **Implemented Proximity Auto-Finish** — 導航中當使用者與終點距離 ≤ 8 米會自動結束騎行流程，無需手動按鈕。
- **Integrated Neon DB Save for Rides & XP** — 自動結束後即時呼叫伺服器函式寫入騎行資料（距離、時間、XP、時間戳）到 Neon PostgreSQL，並同步更新本機玩家等級/XP。
- **Added Post-Ride Map Summary UI** — 新增 SummaryView 卡片，顯示距離、時間、XP 與本次歷史軌跡的非互動地圖回顧。

### 改進 (Improvements)
- **資源清理強化** — 進入 SummaryView 時會清理 GPS watcher 與路線特效計時器，避免結束後仍持續追蹤。

---

## v1.9 — 2026-05-08

### 文件更新 (Documentation)
- **Discord 教學全面重寫** — `discord.md` 由簡版說明升級為完整操作手冊，內容重整為由零開始可直接落地的流程：
  - 新增「先理解架構」章節，清楚拆分網站端與 Bot 端職責及互通方式
  - 重新整理三軌系統（車手等級／里程計劃／會員身份）與 Discord 身份組對應表
  - 以實作順序重寫部署流程：建立 Bot、收集 ID、設定雙端環境變數、啟動服務、驗收
  - 明確標示 Token 一致性與 Bot 身份組排序等高風險設定點，降低部署失敗率
  - 補上 API 對接用途說明（`/api/sync-user`、`/api/oauth?action=discord-profile`、`/api/admin-relay`）
  - 新增常見錯誤排查、維運安全建議與最短上線路徑，方便團隊交接與故障定位

---

## v1.8 — 2026-05-06

### 新功能 (New Features)
- **Discord 帳號連結** — 用戶可在儀表板將 CTRC 帳戶與 Discord 帳號連結：
  - 透過 Discord OAuth2 授權，安全綁定雙方帳號
  - 系統自動讀取用戶在 CTRC Discord 伺服器的身份組，並同步升級對應的 CTRC 會員等級
  - 等級採「只升不降」原則：Discord 身份組被移除後，CTRC 等級不受影響
  - 可隨時在儀表板「重新同步身份組」或「解除連結」
  - 身份組對應規則（由管理員設置）：
    - `DISCORD_ADMIN_ROLE_ID` 身份組 → 管理員（admin）
    - `DISCORD_SENIOR_ROLE_ID` 身份組 → 高級會員（senior）

---

## v1.7 — 2026-03-28

### 新功能 (New Features)
- **深色／淺色主題** — 應用程式新增主題切換功能：
  - 預設自動跟隨手機系統的深淺色設定（`prefers-color-scheme`）
  - 可在「我的帳戶 → 顯示設定」中手動切換，設定儲存於本機
  - 不影響網頁版（網頁版維持原有樣式）
- **全域語言切換** — 按下語言按鈕後，偏好語言儲存於本機：
  - 選擇英文後，所有擁有英文版的頁面（首頁、路線、關於等）將自動導向英文版
  - 選擇中文後，英文頁面自動切換回中文版，直至再次手動切換

### 改進 (Improvements)
- **首頁排行榜快捷按鈕** — 在 App 首頁的快捷功能區新增排行榜入口
- **頭像顯示修復** — App 首頁頂部欄及用戶卡片現正確顯示用戶頭像
- **我的帳戶設定頁** — 在個人資料頁面新增「顯示設定」卡片，包含：
  - 亮色／暗色主題切換開關
  - 登出按鈕（方便在 App 內直接登出）

### 修復 (Bug Fixes)
- **簽到前獎勵顯示 Bug** — 修復在伺服器資料同步完成前，每日簽到預覽一律顯示「第 1 天 +20 XP」的問題；同步期間現顯示「獎勵載入中…」，同步完成後才顯示正確的天數及獎勵
- **騎行記錄判定過嚴** — 放寬「行程過短」的判定條件：舊版需少於 2 站 **且** 少於 1 公里才不計錄；新版改為同時滿足少於 2 站、少於 0.5 公里 **且** 少於 3 分鐘才視為過短，解決 GPS 精度較差時合法騎行被誤判為未記錄的問題
- **騎行地圖總站篩選** — 修復選擇部門後地圖上顯示所有總站（含其他部門）的問題；現在選擇將軍澳部只顯示將軍澳部的總站，選擇港島部亦如此

---

## v1.6 — 2026-03-26

### 新功能 (New Features)
- **排行榜** — 全新 `/leaderboard` 頁面，按路線或全部路線查看騎手里程排名：
  - 精美三名獎台視覺（🥇🥈🥉），一覽前三強
  - 第四名起以列表形式顯示，含總里程、騎行次數、平均速度
  - 自動標示「我」的排名，讓你清楚看見自己位置
  - 若未進入前 20，頂部橫幅顯示目前排名
  - 路線篩選器：可切換特定路線或查看全部路線總里程榜
  - 「我的」頁面新增排行榜快捷入口

### 改進 (Improvements)
- **升級動畫全面強化** — 騎行結束升級時的動畫效果大幅升級：
  - 彩色紙屑從畫面頂部灑下（60 枚多色紙屑，含圓形與方形）
  - 新增升級徽章彈出動畫（放大彈跳效果）
  - 新增「已解鎖新路線」提示，升級時即時顯示新解鎖的路線編號
  - 關閉升級畫面時自動清理所有動畫元素，避免記憶體洩漏
- **XP 數字動畫** — 騎行總結頁面的 XP 獎勵數字現在從 0 逐漸數到目標值（緩動效果），視覺效果更有趣
- **簽到彩蛋動畫** — 每日簽到成功後觸發彩色紙屑慶祝動畫（40 枚）
- **簽到升級動畫** — 若每日簽到時剛好升級，畫面將顯示與騎行相同的升級彈窗（含大量紙屑）
- **Service Worker 更新** — 版本升至 v2.4.0，預緩存範圍新增 `/leaderboard` 頁面

---

## v1.5.2.3 — 2026-03-25

### 修復 (Bug Fixes)
- **聊天訊息氣泡文字換行修復** — 修復聊天訊息每個字元各佔一行的排版問題；根本原因：`.msg-bubble` 的 `max-width: 70%` 是相對其無明確寬度的父 `div` 計算，形成循環依賴，瀏覽器解析後幾乎為零寬度；已將 `max-width`移至新增的 `.msg-inner` flex 子項層級，使百分比相對 `.msg-bubble-wrap` 的寬度正確計算，並補充 `overflow-wrap: break-word` 提升相容性
- **聊天切換用戶延遲修復** — 修復切換對話時停留在舊用戶畫面數秒的問題；新增記憶體快取（`msgCache`，以 `peerId` 為鍵）：切換對話時立即從快取渲染上次訊息，同時在背景更新最新資料；若使用者在回應抵達前已切換至其他對話，舊回應將被捨棄，避免訊息錯亂

---

## v1.5.2.1 — 2026-03-25

### 修復 (Bug Fixes)
- **聊天頁手機版全面修復** — 修復手機版聊天功能多個核心故障：
  - **對話列表點擊無反應** — 根本原因：`renderConvList` 以 `JSON.stringify` 直接插入 `onclick` HTML 屬性，雙引號破壞屬性值導致事件失效；已將輸出字串的雙引號改以 `&quot;` HTML 實體編碼
  - **新對話用戶搜尋無反應** — 同上，`renderUserResults` 中相同問題已一併修復
  - **對話搜尋框無反應** — `filterConvList` 未暴露至全域（`window`），導致 `oninput` 處理器呼叫失敗；已加入 `window.filterConvList = filterConvList`
  - **關閉對話列表後無法復原** — 舊版 CSS 錯誤地將 `btn-new-chat` 類別在所有手機介面下隱藏，致使「查看對話」及「新對話」捷徑按鈕消失；已改為全螢幕滑入式面板設計：對話列表以滑動方式全螢幕展示，不再依賴覆蓋層定位，消除 z-index 衝突
  - **漢堡選單（三橫線）按下後無法返回** — 已於手機版新增返回按鈕（訊息區頂部），並確保空白狀態的捷徑按鈕正常顯示

---

## v1.5.2 — 2026-03-25

### 修復 (Bug Fixes)
- **聊天頁手機適配修復** — 修復手機（≤600px 螢幕）無法發起及查看聊天的問題；對話列表改為可開合的抽屜式側欄，進入頁面時自動展開、選取對話後自動收起；新增「返回列表」按鈕（訊息頁頂部）及「查看對話」捷徑（空白訊息區），手機用戶可順暢使用所有聊天功能

### 改進 (Improvements)
- **減少頁面載入等待** — 「我的」頁面現在即時從本機快取顯示遊戲進度（等級、XP、里程幣），後台同步完成後再更新數值，不再需要等待 API 回應才顯示內容
- **擴大離線緩存範圍** — Service Worker 預緩存新增 `/nav`（導航）、`/chat`（聊天）、`/tasks`（任務）、`/forum`（論壇）頁面，首次載入後切換至這些頁面速度更快

---

## v1.5.1 — 2026-03-25

### 修復 (Bug Fixes)
- **任務頁即時顯示** — 任務頁不再等待伺服器同步完成才渲染；頁面現在立即從本地快取顯示，背景同步完成後再更新，大幅改善載入體感
- **簽到按鈕載入狀態** — 同步期間簽到按鈕會顯示「載入中…」，避免在伺服器資料抵達前誤顯示「第 1 天」獎勵資訊

---

## v1.5.0 — 2026-03-24

### 新功能 (New Features)
- **單車徑導航升級至 Brouter 路由引擎（方案 C）** — 導航頁 `/nav` 的路線規劃引擎由通用 OSRM 改為 [Brouter](https://brouter.de)，專為單車優化，優先選擇單車徑及低流量道路，路線更安全準確；無需 API Key，完全免費
- **路線偏好選擇器** — 規劃路線時可選擇四種騎行風格：安全路線（優先單車徑）、遠足路線（混合路面）、快速路線（最快到達）、最短路線（最短距離）
- **OSRM 自動降級備援** — 若 Brouter 服務暫時不可用，系統會自動切換至 OSRM 公開 API 作為備用，確保導航功能持續可用
- **幾何轉向偵測** — 轉向指示改由路線幾何坐標自動計算（取代 OSRM 指令），在任何路由引擎下均可提供轉向提示

---

## v1.4.2 — 2026-03-22

### 修復 (Bug Fixes)
- **主頁進度條載入修復** — 當 `/api/getHistory` 暫時失敗時，主頁會以本機快取資料回填等級/XP/里程幣，不再固定顯示 0%
- **主頁聊天快捷按鈕排版修復** — 聊天按鈕圖示固定置頂顯示（圖示在上、文字在下）
- **最近騎行 500 錯誤容錯** — `/api/getHistory` 針對缺失資料表加入存在性檢查與降級回應，主頁與「我的」不再因資料表未就緒而 server error 500
- **聊天啟動流程修復** — 新增註冊用戶名欄位（4-16 位英數/底線且不可重複）；既有帳戶首次使用聊天會先要求設定用戶名，並可直接輸入對方用戶名開始新對話
- **聊天來訊通知修復** — 收到新訊息時（App 在背景）會觸發瀏覽器通知提示
- **iOS 通知文字精簡** — 移除推送預設標題中的 App 名稱，避免通知中出現多餘「from 城市運輸單車」字樣
- **導航自訂地址修復** — `/nav` 起終點除預設地標外，現可直接輸入自訂地址並進行地理編碼後規劃路線

---

## v1.4.1 — 2026-03-22

### 修復 (Bug Fixes)
- **任務/獎牌帳戶同步** — 任務頁現在會先同步伺服器騎行歷史，再計算任務與獎牌進度；重裝 App 後登入亦可恢復進度
- **導航頁底部欄修復** — `/nav` 頁面現在載入 App 底部導航樣式，並調整規劃面板/GPS 位置避免與底欄互相遮擋
- **主頁新增聊天入口** — App 主頁快捷功能新增「聊天」按鈕，方便快速進入 `/chat`
- **導航起點/終點可輸入** — 導航頁改為可輸入/搜尋地標（含「使用目前位置」）後再規劃路線
- **部門解鎖後隱藏價格** — 成功購買解鎖後，部門卡片不再顯示原價/優惠價
- **港島海濱部總站篩選補全** — 地圖新增北角碼頭、北角碼頭巴士總站、東岸板道（東/西）作為終點篩選點
- **主頁底部可完整滑動** — App 主頁增加底部安全間距，避免內容被底部導航欄遮住
- **頭像大小限制放寬** — 頭像上傳上限由 200KB 提升到 1MB（前後端一致）
- **App 更新體驗改善** — Service Worker 更新時加入「正在更新」提示並快速自動重載
- **部門選單可直接關閉** — 部門彈層關閉按鈕新增可及性標記與事件保險處理，點擊 × 可直接關閉而不需強制選擇

---

## v1.4.0 — 2026-03-22

### 新功能 (New Features)
- **個人頭像設定** — 用戶可在帳戶設定頁面上傳個人頭像（支援 JPG、PNG、GIF、WebP，最大 150KB），頭像將顯示於聊天介面及個人資料中
- **用戶聊天功能** — 全新 `/chat` 頁面，支援用戶之間互相發送即時訊息；功能包括：
  - 對話列表（顯示最近聯絡人、最後一條訊息及未讀數量）
  - 即時訊息輪詢（每 3秒自動刷新）
  - 向上捲動載入更早的訊息記錄
  - 用戶搜尋功能（開始新對話）

### 修復 (Bug Fixes)
- **PWA 自動更新** — 已安裝的 App 在有新版本發布後，會自動重新載入頁面以套用最新版本，不再需要手動重新整理
- **App 名稱更正** — 新用戶安裝 PWA App 時，App 名稱由「香港城市運輸單車」更正為「城市運輸單車」

### 改進 (Improvements)
- **下載頁面更新** — 更新 iOS 安裝說明，說明 Safari 為首選瀏覽器，部分較新版本 Chrome for iOS 亦支援安裝 PWA
- **下載頁面功能描述** — 更新「網頁和 App 有什麼分別？」說明，清楚描述 App 版獨有功能（底部導航欄、全螢幕體驗、離線瀏覽、每日簽到提醒通知等）；App 功能特色一覽新增「用戶聊天」及「個人頭像」項目

---

## v1.3.0 — 2026-03-22

### 新功能 (New Features)
- **App 底部導航重排** — 導航欄順序改為：主頁 → 任務 → 騎行 → 導航 → 我的，令常用功能更易觸及
  - 「我的」整合原「進度」頁面及所有「更多」快速連結（關於我們、會員計劃、部落格、聯絡我們、切換語言、登出等）
  - App 預設啟動頁面由「進度」改為「主頁」
- **管理員推送通知目標細化** — 發放通知時新增「已解鎖指定部門」及「已解鎖指定路線」兩種目標選項，可精準推送給特定用戶群組

### 改進 (Improvements)
- **啟動畫面優化** — 加載畫面僅於 App 冷啟動（首次開啟）時顯示；在已開啟的 App 內導航至主頁，不再重複顯示加載畫面；切換回前台時靜默刷新不彈出加載頁
- **移除路線頁單車徑導航按鈕** — 路線頁（巴士路線）不再顯示「單車徑導航」按鈕，避免混淆

### 安全改進 (Security)
- **未驗證帳戶強制驗證** — 電郵尚未驗證的帳戶無法登入或使用 App，必須先完成電郵驗證才能正常使用所有功能

---

## v1.2.0 — 2026-03-21

### 新功能 (New Features)
- **新部門：港島海濱部** — 全新港島北岸海濱單車路線部門正式上線！
  - 推廣期限時優惠：只需 🪙20 里程幣即可解鎖（原價 🪙400）
  - 解鎖部門後，以下路線免費騎行

- **路線 7E — 北角碼頭來往東岸板道（東）** — 沿港島北岸東行，1.0km，約 6 分鐘，共 7 個站點

- **路線 7W — 北角碼頭來往東岸板道（西）** — 沿港島北岸西行，1.0km，約 6 分鐘，共 5 個站點

- **管理員路線管理** — 管理後台新增「路線管理」功能，可隨時推出路線里程幣優惠及部門解鎖推廣活動

### 修復 (Bug Fixes)
- **修復 GPS 漂移** — 設定最小移動距離門檻（3 米），防止靜止時 GPS 輕微鋪動造成騎行距離不斷增加
- **修復單車徑導航自訂起點終點** — 導航頁新增「使用目前位置」選項，讓用戶可選擇當前 GPS 位置作為導航起點或終點

---

## v1.1.0 — 2026-03-21

### 新功能 (New Features)
- **騎行介面重設計**：將原先需要左右滑動切換的兩頁式騎行介面，改為單一頁面——所有騎行資訊（時間、距離、站點、速度、XP 等）以浮動面板形式疊加於地圖上，無需切換頁面即可查看。
  - 底部浮動面板常態顯示關鍵數據（時間、距離、已到站點數）。  
  - 點擊面板或上拉手柄，可展開查看完整騎行數據、站點進度及「結束騎行」按鈕。

- **自由選擇出發站點**：騎行前「前往起點」指引介面新增站點選擇器，用戶可自行選擇任意站點作為出發站，不再局限於路線起始站。
  - GPS 自動偵測仍有效：抵達所選站點後自動開始計時。
  - 可直接點選「從當前位置出發」立即開始騎行，或選定站點後點選「從選定站點出發」跳過導航。

---

## v1.0.1 — 2026-03-21

### 修復 (Bug Fixes)
- **電郵驗證**：修復新用戶完成註冊後未能收到驗證電郵的問題。  
  根本原因：`api/register.js` 原本以非阻塞（fire-and-forget）方式發送驗證郵件，在 Vercel serverless 環境中，系統會在回傳回應後立即終止函式執行，導致尚未完成的 SMTP 連線被中斷。  
  解決方法：改為 `await` 等待郵件發送完成後再回傳回應，確保郵件能成功送出。

---

## v1.0.0 — 2026-03-01

### 初始版本 (Initial Release)
- 用戶註冊與電郵驗證系統
- Google OAuth 登入
- 騎行路線瀏覽與詳情頁面
- PWA 騎行遊戲系統（GPS 打卡、XP、等級制度）
- 每日簽到系統
- 用戶個人資料與高級會員升級
- 論壇系統
- 管理後台
- 推送通知（Web Push）


## GOOGLE SITES VER. [ARCHIVED]

- 無任何可用修改
